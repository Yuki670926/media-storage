# CLAUDE.md — このリポジトリで実装を進める Claude Code へ

## プロジェクト概要

iPhone の写真・動画を、運用者自身が管理するサーバーへ自動バックアップする個人用システム。現在は**設計フェーズ完了・実装未着手**。あなた(Claude Code)が運用者と対話しながら実装を進める。

## 最初に読む順番

1. [docs/REQUIREMENTS.md](docs/REQUIREMENTS.md) — 要件定義。**§2 の非機能要件の優先順位(耐久性 > 完全性 > 出口戦略 > … > 可用性)がすべての設計・実装判断の基準**
2. [docs/DESIGN.md](docs/DESIGN.md) — アーキテクチャ、データモデル、アップロードプロトコル、フェーズ計画
3. [docs/HARDWARE.md](docs/HARDWARE.md) — 機材・コスト計画

## 実装を始める前に(必須)

REQUIREMENTS.md **§5.2 のチェックリストを運用者と対話して確定**させること。未確定のまま実装を始めない。確定した内容はチェックボックスを埋めて(必要なら仮置き値を書き換えて)コミットする。

## 鉄則(要件から導出。破る場合は先に運用者へ確認)

- オリジナルのバイト列は**無加工**で保存する。変換・圧縮・再エンコードして保存しない
- 認証なしでメディア・メタデータを返すエンドポイントを**作らない**(未認証には 1 バイトも返さない)
- 重複判定は SHA-256 のみ。ファイル名・日時での判定を書かない
- 派生ファイル(サムネイル・プレビュー)は「全部消しても再生成できる」ように書く
- 単一ノード前提。HA・クラスタ・分散機構を持ち込まない(可用性は要件の最下位)
- DESIGN.md §8 のフェーズ順(Phase 0 → 1 → 2 → 3)を守り、各フェーズを「単体で使える状態」で完了させる

## フェーズごとの完了条件(Done の定義)

- **Phase 0**: `docker compose up -d` で API + DB が起動し、CLI から写真をアップロードすると `originals/` に保存され、同じファイルの再アップロードが check API でスキップされる
- **Phase 1**: iPhone 実機から手動バックアップが完走し、進捗・エラーが画面で確認できる
- **Phase 2**: 充電中の自動差分バックアップと、外出先からの Web タイムライン閲覧が動く
- **Phase 3**: アルバム同期・エクスポートツール等(詳細は DESIGN.md §8)

## ドキュメントと実装の関係

矛盾を見つけたら **REQUIREMENTS.md が勝つ**(次いで DESIGN.md)。設計を変えたくなったら、docs/ の該当文書を先に更新してから実装する。ドキュメントに書いていない大きな判断をコードだけに埋め込まない。
